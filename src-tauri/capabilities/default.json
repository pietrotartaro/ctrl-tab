{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the overlay panel and the settings window",
  "windows": ["overlay", "settings"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}

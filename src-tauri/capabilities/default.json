{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the dev-controls window and the overlay panel",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
